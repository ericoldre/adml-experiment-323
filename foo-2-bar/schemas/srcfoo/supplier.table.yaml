apiVersion: adml/v0
kind: table-schema

id: supplier
description: "Supplier table translated from the source SQL Server definition to the closest Postgres-compatible schema."
database: foo

columns:
  - name: SupplierID
    type: varchar(15)
    nullable: false
    key: true

  - name: Supplier
    type: varchar(50)
    nullable: false

  - name: AddressType
    type: varchar(50)
    nullable: false

  - name: AddressLine1
    type: varchar(60)
    nullable: true

  - name: AddressLine2
    type: varchar(60)
    nullable: true

  - name: City
    type: varchar(30)
    nullable: true

  - name: StateCode
    type: varchar(3)
    nullable: true

  - name: PostalCode
    type: varchar(15)
    nullable: true

  - name: CountryCode
    type: varchar(3)
    nullable: true

  - name: PhoneNumber
    type: varchar(25)
    nullable: true

  - name: ActiveFlag
    type: boolean
    nullable: false

  - name: CreatedDate
    type: date
    nullable: true

  - name: CreatedBy
    type: varchar(50)
    nullable: true