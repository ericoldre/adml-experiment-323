apiVersion: adml/v0
kind: plan

id: migrate-orders
title: "Migrate Orders"
description: "Applies the working-database schema, then transforms order data."

entries:
  - task: apply-working-schema
    remarks: "Ensure working-database tables exist and match declarations."

  - task: orders-mapping
    remarks: "Apply field-level mapping rules for Orders."
