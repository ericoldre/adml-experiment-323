apiVersion: adml/v0
kind: table-schema
id: customer_target

description: "Target-side customer table referenced by the working-table methodology placeholders."

columns:
  - name: CUSTOMER_ID
    type: integer
    nullable: false
    key: true

  - name: CUSTOMER_NAME
    type: varchar(100)
    nullable: false
